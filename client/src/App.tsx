import { useState, type FormEvent } from "react";
import {
  Activity,
  ArrowRight,
  Bell,
  Building2,
  CalendarDays,
  Check,
  ChevronDown,
  CircleDollarSign,
  Dumbbell,
  FileText,
  Headphones,
  LayoutDashboard,
  LogOut,
  Menu,
  MessageCircle,
  Moon,
  MoreHorizontal,
  Play,
  Settings,
  ShieldCheck,
  Sparkles,
  TrendingUp,
  UserRound,
  Users,
  Utensils,
  WalletCards,
  X,
  Zap,
} from "lucide-react";
import { Button } from "@/components/ui/button";
import { Badge } from "@/components/ui/badge";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { Separator } from "@/components/ui/separator";
import { cn } from "@/lib/utils";
import { ALL_DEMO_USERS, DEMO_CLIENTS, DEMO_MODULE_USERS, DEMO_PASSWORD, DEMO_USERS, MODULE_LABELS, MODULE_PERMISSIONS, MODULE_VIEWS, getDemoUser, getModuleDemoUser, type DemoUser, type ModuleKey } from "@/lib/arkeDemo";
import Home from "@/pages/Home";

export type ViewKey = "overview" | "academias" | "profissionais" | "alunos" | "agenda" | "financeiro" | "integracoes" | "saas" | "admin" | "configuracoes";
type UserProfile = DemoUser;

function LoginScreen({ onLogin }: { onLogin: (user: UserProfile, remember: boolean) => void }) {
  const [module, setModule] = useState<ModuleKey>("administrador");
  const [identifier, setIdentifier] = useState("andre.alvesman@gmail.com");
  const [password, setPassword] = useState("12345678");
  const [error, setError] = useState("");
  const [showPassword, setShowPassword] = useState(false);
  const [remember, setRemember] = useState(true);
  const [showRecovery, setShowRecovery] = useState(false);
  const [recoveryIdentifier, setRecoveryIdentifier] = useState("");
  const [recoverySent, setRecoverySent] = useState(false);
  const submit = (event: FormEvent) => { event.preventDefault(); const user = module === "administrador" ? getModuleDemoUser(module, identifier, password) ?? getDemoUser(identifier, password) : getModuleDemoUser(module, identifier, password); if (!user) return setError(`Usuário ou senha inválidos para o módulo ${MODULE_LABELS[module]}.`); onLogin(user, remember); };
  return <div className="min-h-screen overflow-hidden bg-[#f7f5f0] text-[#231f18]"><div className="grid min-h-screen lg:grid-cols-[1.08fr_.92fr]"><section className="relative hidden overflow-hidden bg-[#15130f] lg:block"><div className="absolute inset-0 opacity-50 [background-image:radial-gradient(#6bcb8d_1px,transparent_1px)] [background-size:28px_28px]" /><div className="absolute -left-24 -top-20 h-80 w-80 rounded-full bg-[#d4a017]/15 blur-3xl" /><div className="absolute bottom-0 right-0 h-[440px] w-[440px] rounded-full bg-[#5ac87f]/15 blur-3xl" /><div className="relative flex h-full flex-col justify-between p-12 xl:p-16"><div className="flex items-center gap-3 text-white"><div className="flex h-11 w-11 items-center justify-center overflow-hidden rounded-2xl bg-black shadow-[0_10px_30px_rgba(0,0,0,.25)]"><img src="/manus-storage/arke-logo-cropped_38a431ec.png" alt="Logo Arke" className="h-full w-full object-cover" /></div><span className="text-2xl font-black tracking-[-.06em]">ARKE</span></div><div className="max-w-xl text-white"><p className="mb-6 inline-flex items-center gap-2 rounded-full border border-white/15 bg-white/10 px-3 py-1.5 text-xs font-semibold uppercase tracking-[.18em] text-[#d4a017]"><Sparkles size={13} /> cockpit de crescimento</p><h1 className="max-w-2xl text-5xl font-semibold leading-[.98] tracking-[-.06em] xl:text-7xl">Gestão, treino e cuidado em um só ecossistema.</h1><p className="mt-7 max-w-lg text-base leading-7 text-[#d5cfc1]">Uma plataforma leve para academias, profissionais e alunos. Pronta para apresentar, validar e licenciar.</p><div className="mt-10 grid max-w-lg grid-cols-3 gap-3">{[["03", "módulos conectados"], ["100%", "modo apresentação"], ["24h", "agenda inteligente"]].map(([value, label]) => <div key={label} className="rounded-2xl border border-white/10 bg-white/5 p-4"><p className="text-2xl font-semibold text-[#d4a017]">{value}</p><p className="mt-1 text-xs leading-4 text-[#bed1c5]">{label}</p></div>)}</div></div><p className="text-xs text-[#9bb9a8]">ARKE • Sistema de operação e acompanhamento • Demo investor-ready</p></div></section><section className="flex items-center justify-center px-6 py-10 sm:px-10"><div className="w-full max-w-md"><div className="mb-10 flex items-center gap-3 lg:hidden"><div className="flex h-10 w-10 items-center justify-center overflow-hidden rounded-xl bg-black"><img src="/manus-storage/arke-logo-cropped_38a431ec.png" alt="Logo Arke" className="h-full w-full object-cover" /></div><span className="text-xl font-black tracking-[-.05em]">ARKE</span></div><div className="mb-8"><p className="mb-3 text-sm font-semibold text-[#a47b13]">Acesso por módulo</p><h2 className="text-4xl font-semibold tracking-[-.05em] text-[#231f18]">Entre no seu Arke</h2><p className="mt-3 text-sm leading-6 text-[#766f62]">Escolha seu módulo para abrir diretamente a experiência certa.</p></div><form onSubmit={submit} className="space-y-5 rounded-3xl border border-[#e4e0d7] bg-white p-6 shadow-[0_20px_70px_rgba(32,58,45,.08)] sm:p-8"><div><label className="mb-2 block text-sm font-medium text-[#574e40]">Módulo</label><select value={module} onChange={(e) => { const next = e.target.value as ModuleKey; setModule(next); setIdentifier(next === "administrador" ? "andre.alvesman@gmail.com" : DEMO_MODULE_USERS.find((user) => user.module === next)?.username ?? ""); setError(""); }} className="h-12 w-full rounded-xl border border-[#dfe8e1] bg-[#fffdf9] px-3 text-sm text-[#574e40] outline-none">{Object.entries(MODULE_LABELS).map(([key, label]) => <option key={key} value={key}>{label}</option>)}</select></div><div><label className="mb-2 block text-sm font-medium text-[#574e40]">{module === "administrador" ? "Usuário ou e-mail" : "Usuário"}</label><Input value={identifier} onChange={(e) => setIdentifier(e.target.value)} type="text" placeholder={module === "administrador" ? "administrador ou e-mail" : "nome do usuário"} className="h-12 rounded-xl border-[#dfe8e1] bg-[#fffdf9]" /></div><div><div className="mb-2 flex items-center justify-between"><label className="block text-sm font-medium text-[#574e40]">Senha</label><button type="button" onClick={() => setShowPassword(!showPassword)} className="text-xs font-semibold text-[#a47b13]">{showPassword ? "Ocultar" : "Exibir"}</button></div><Input value={password} onChange={(e) => setPassword(e.target.value)} type={showPassword ? "text" : "password"} className="h-12 rounded-xl border-[#dfe8e1] bg-[#fffdf9]" /></div><label className="flex items-center gap-2 text-xs text-[#766f62]"><input type="checkbox" checked={remember} onChange={(event) => setRemember(event.target.checked)} className="h-4 w-4 accent-[#b08317]" /> Manter conectado neste dispositivo</label>{error && <div className="rounded-xl bg-[#f8e6df] px-3 py-2 text-xs font-medium text-[#b65343]">{error}</div>}<Button type="submit" className="h-12 w-full rounded-xl bg-[#15130f] text-sm font-semibold text-white shadow-[0_8px_20px_rgba(21,59,43,.18)] hover:bg-[#2b271d]">Entrar no Arke <ArrowRight size={16} /></Button><button type="button" onClick={() => { setShowRecovery(!showRecovery); setRecoverySent(false); }} className="w-full text-center text-xs font-semibold text-[#a47b13]">{showRecovery ? "Voltar ao login" : "Esqueci minha senha"}</button>{showRecovery && <div className="space-y-3 rounded-2xl bg-[#faf7ef] p-4"><p className="text-xs leading-5 text-[#766f62]">Informe seu usuário ou e-mail. Em produção, enviaremos um link seguro; aqui o envio é simulado.</p><Input value={recoveryIdentifier} onChange={(event) => setRecoveryIdentifier(event.target.value)} placeholder="usuário ou e-mail" className="h-10 rounded-xl border-[#e2dcca] bg-white text-xs" /><Button type="button" onClick={() => setRecoverySent(true)} className="h-10 w-full rounded-xl bg-[#d4a017] text-xs font-semibold text-[#15130f] hover:bg-[#b98b13]">{recoverySent ? "Link simulado enviado" : "Enviar link de recuperação"}</Button></div>}<div className="flex items-center gap-3 py-1"><div className="h-px flex-1 bg-[#e4e0d7]" /><span className="text-[10px] font-semibold uppercase tracking-[.16em] text-[#a1aca5]">contas demo</span><div className="h-px flex-1 bg-[#e4e0d7]" /></div><div className="space-y-2">{[...DEMO_USERS, ...DEMO_MODULE_USERS].map((demo) => <button type="button" key={demo.email} onClick={() => { setModule(demo.module); setIdentifier(demo.username ?? demo.email); setPassword(DEMO_PASSWORD); setError(""); }} className="flex w-full items-center gap-3 rounded-xl border border-[#e9e5dc] p-3 text-left transition hover:border-[#d4a017] hover:bg-[#fffaf0]"><div className="flex h-8 w-8 items-center justify-center rounded-full bg-[#f2e5c4] text-[11px] font-bold text-[#35704a]">{demo.initials}</div><div className="min-w-0 flex-1"><p className="truncate text-xs font-semibold text-[#33463a]">{demo.username ?? demo.email}</p><p className="text-[11px] text-[#9b9488]">{MODULE_LABELS[demo.module]} • senha {DEMO_PASSWORD}</p></div><ArrowRight size={14} className="text-[#a1b0a6]" /></button>)}</div></form><p className="mt-7 text-center text-xs text-[#95a098]">Ambiente de apresentação • Dados simulados e seguros</p></div></section></div></div>;
}

function PasswordDialog({ user, onClose }: { user: UserProfile; onClose: () => void }) {
  const [current, setCurrent] = useState(""); const [nextPassword, setNextPassword] = useState(""); const [confirmation, setConfirmation] = useState(""); const [message, setMessage] = useState("");
  const save = () => { const saved = localStorage.getItem(`arke-demo-password:${user.email}`) || DEMO_PASSWORD; if (current !== saved) return setMessage("A senha atual não confere."); if (nextPassword.length < 8 || nextPassword !== confirmation) return setMessage("A nova senha precisa ter 8 caracteres e confirmar corretamente."); localStorage.setItem(`arke-demo-password:${user.email}`, nextPassword); setMessage("Senha alterada neste dispositivo."); window.setTimeout(onClose, 900); };
  return <div className="fixed inset-0 z-50 flex items-center justify-center bg-[#15130f]/40 p-5"><div className="w-full max-w-md rounded-3xl border border-[#e4e0d7] bg-[#fffdf9] p-6 shadow-2xl"><div className="flex items-start justify-between"><div><p className="text-xs font-bold uppercase tracking-[.14em] text-[#a47b13]">Segurança</p><h2 className="mt-2 text-2xl font-semibold tracking-[-.04em] text-[#2b271f]">Alterar senha</h2></div><button onClick={onClose} className="text-[#9b9488]">×</button></div><div className="mt-5 space-y-3"><Input type="password" value={current} onChange={(event) => setCurrent(event.target.value)} placeholder="Senha atual" className="h-11 rounded-xl border-[#e2dcca] bg-white" /><Input type="password" value={nextPassword} onChange={(event) => setNextPassword(event.target.value)} placeholder="Nova senha" className="h-11 rounded-xl border-[#e2dcca] bg-white" /><Input type="password" value={confirmation} onChange={(event) => setConfirmation(event.target.value)} placeholder="Confirmar nova senha" className="h-11 rounded-xl border-[#e2dcca] bg-white" />{message && <p className="rounded-xl bg-[#f5ead0] px-3 py-2 text-xs text-[#80641f]">{message}</p>}<Button onClick={save} className="h-11 w-full rounded-xl bg-[#15130f] text-sm font-semibold text-white hover:bg-[#2b271d]">Salvar nova senha</Button></div></div></div>;
}

const navGroups = [
  { label: "Visão geral", items: [{ key: "overview", label: "Dashboard", icon: LayoutDashboard }] },
  { label: "Módulos", items: [{ key: "academias", label: "Academia", icon: Building2 }, { key: "profissionais", label: "Profissionais", icon: UserRound }, { key: "alunos", label: "App do aluno", icon: Dumbbell }] },
  { label: "Operação", items: [{ key: "agenda", label: "Agenda e turmas", icon: CalendarDays }, { key: "financeiro", label: "Financeiro", icon: CircleDollarSign }, { key: "integracoes", label: "Integrações teste", icon: Zap }] },
  { label: "SaaS", items: [{ key: "saas", label: "Licenças & planos", icon: ShieldCheck }, { key: "admin", label: "Clientes & usuários", icon: Users }] },
];

function Sidebar({ view, setView, user, workspace, module, onModuleChange, onPasswordChange, onLogout }: { view: ViewKey; setView: (view: ViewKey) => void; user: UserProfile; workspace: string; module: ModuleKey; onModuleChange: (module: ModuleKey) => void; onPasswordChange: () => void; onLogout: () => void }) {
  let userPermissions: string[] | undefined; try { userPermissions = JSON.parse(localStorage.getItem("arke-user-permissions") || "null")?.[user.email]; } catch { userPermissions = undefined; }
  const allowedPermissions = userPermissions ?? MODULE_PERMISSIONS[module];
  const canSwitchModule = user.role === "Super Admin";
  const visibleNavGroups = navGroups.map((group) => ({ ...group, items: group.items.filter((item) => allowedPermissions.includes(item.key)) })).filter((group) => group.items.length > 0);
  return <aside className="hidden w-[252px] shrink-0 flex-col border-r border-[#e4e0d7] bg-[#fffdf9] px-4 py-5 lg:flex"><div className="flex items-center gap-3 px-3"><div className="flex h-9 w-9 items-center justify-center overflow-hidden rounded-xl bg-black"><img src={user.logoUrl ?? "/manus-storage/arke-logo-cropped_38a431ec.png"} alt="Logo do cliente" className="h-full w-full object-cover" /></div><span className="text-lg font-black tracking-[-.06em] text-[#1a2b21]">ARKE</span><span className="rounded-md bg-[#f5ead0] px-1.5 py-0.5 text-[9px] font-bold uppercase tracking-wider text-[#a47b13]">PRO</span></div><div className="mt-8 rounded-2xl border border-[#e4e0d7] bg-white p-3 shadow-[0_5px_20px_rgba(41,76,54,.04)]"><p className="px-2 text-[10px] font-bold uppercase tracking-[.16em] text-[#9aaa9f]">Módulo ativo</p><select disabled={!canSwitchModule} value={module} onChange={(event) => onModuleChange(event.target.value as ModuleKey)} className="mt-2 h-10 w-full rounded-xl bg-[#faf3df] px-2 text-xs font-semibold text-[#314238] outline-none">{(canSwitchModule ? Object.entries(MODULE_LABELS) : [[module, MODULE_LABELS[module]]]).map(([key, label]) => <option key={key} value={key}>{label}</option>)}</select><button onClick={() => setView("saas")} className="mt-2 flex w-full items-center gap-2 rounded-xl bg-[#faf3df] p-2 text-left"><div className="flex h-8 w-8 items-center justify-center rounded-lg bg-[#f2e5c4] text-[#3e8254]"><Building2 size={16} /></div><div className="min-w-0 flex-1"><p className="truncate text-xs font-semibold text-[#314238]">{workspace}</p><p className="text-[10px] text-[#8b9b90]">{canSwitchModule ? "Conta SaaS ativa" : "Subsistema do cliente"}</p></div><ChevronDown size={14} className="text-[#8ca092]" /></button></div><nav className="mt-7 flex-1 space-y-6 overflow-y-auto">{visibleNavGroups.map((group) => <div key={group.label}><p className="mb-2 px-3 text-[10px] font-bold uppercase tracking-[.15em] text-[#aaa193]">{group.label}</p><div className="space-y-1">{group.items.map((item) => { const Icon = item.icon; return <button key={item.key} onClick={() => setView(item.key as ViewKey)} className={cn("flex w-full items-center gap-3 rounded-xl px-3 py-2.5 text-left text-sm transition", view === item.key ? "bg-[#15130f] font-semibold text-white shadow-[0_7px_16px_rgba(21,59,43,.15)]" : "text-[#766f62] hover:bg-[#f4f0e7] hover:text-[#264834]")}><Icon size={17} strokeWidth={view === item.key ? 2.4 : 1.8} /><span>{item.label}</span>{item.key === "integracoes" && <span className="ml-auto rounded-full bg-[#d4a017] px-1.5 py-0.5 text-[9px] font-bold text-[#24472f]">TESTE</span>}</button> })}</div></div>)}</nav><div className="space-y-2 border-t border-[#e4e0d7] pt-4"><button onClick={() => setView("configuracoes")} className={cn("flex w-full items-center gap-3 rounded-xl px-3 py-2.5 text-sm", view === "configuracoes" ? "bg-[#f6efdf] font-semibold text-[#315e40]" : "text-[#748278] hover:bg-[#f4f0e7]")}><Settings size={17} /> Configurações</button><button onClick={onPasswordChange} className="w-full rounded-xl px-3 py-2 text-left text-xs font-semibold text-[#a47b13] hover:bg-[#faf3df]">Alterar senha</button><div className="flex items-center gap-3 rounded-xl bg-[#f6f2e9] p-2"><div className="flex h-8 w-8 items-center justify-center rounded-full bg-[#15130f] text-[10px] font-bold text-[#d4a017]">{user.initials}</div><div className="min-w-0 flex-1"><p className="truncate text-xs font-semibold text-[#34473a]">{user.name}</p><p className="truncate text-[10px] text-[#8b998f]">{user.role}</p></div><button onClick={onLogout} aria-label="Sair"><LogOut size={15} className="text-[#91a098]" /></button></div></div></aside>;
}

function Topbar({ view, user, workspace, onMenu, onLogout }: { view: ViewKey; user: UserProfile; workspace: string; onMenu: () => void; onLogout: () => void }) {
  const labels: Record<ViewKey, string> = { overview: "Dashboard", academias: "Academia", profissionais: "Profissionais", alunos: "App do aluno", agenda: "Agenda e turmas", financeiro: "Financeiro", integracoes: "Integrações teste", saas: "Licenças & planos", admin: "Clientes & usuários", configuracoes: "Configurações" };
  return <header className="flex h-[74px] items-center justify-between border-b border-[#e9e5dc] bg-[#fffdf9]/90 px-5 backdrop-blur-md sm:px-8"><div className="flex items-center gap-3"><button onClick={onMenu} className="rounded-lg p-2 text-[#65766a] hover:bg-[#f4f0e7] lg:hidden"><Menu size={20} /></button><div><p className="text-[11px] font-medium text-[#98a69c]">{workspace} <span className="mx-1">/</span> <span className="text-[#50645a]">{labels[view]}</span></p><h1 className="mt-1 text-lg font-semibold tracking-[-.03em] text-[#2b271f]">{labels[view]}</h1></div></div><div className="flex items-center gap-2 sm:gap-4"><div className="hidden items-center gap-2 rounded-full border border-[#e1e9e2] bg-white px-3 py-1.5 text-xs text-[#766f62] sm:flex"><span className="h-2 w-2 animate-pulse rounded-full bg-[#d4a017]" /> {user.role === "Super Admin" ? "SaaS demo ativo" : "Subsistema isolado"}</div><button className="relative rounded-xl p-2 text-[#766f62] hover:bg-[#f4f0e7]"><Bell size={18} /><span className="absolute right-1.5 top-1.5 h-1.5 w-1.5 rounded-full bg-[#b95f3e]" /></button><div className="hidden h-8 w-px bg-[#e4e0d7] sm:block" /><button onClick={onLogout} className="flex items-center gap-2 rounded-xl p-1.5 hover:bg-[#f4f0e7]"><div className="flex h-8 w-8 items-center justify-center rounded-full bg-[#f2e5c4] text-[10px] font-bold text-[#3e8054]">{user.initials}</div><ChevronDown size={14} className="hidden text-[#8a988e] sm:block" /></button></div></header>;
}

function MobileNav({ view, setView, module }: { view: ViewKey; setView: (view: ViewKey) => void; module: ModuleKey }) {
  const items = [{ key: "overview", label: "Início", icon: LayoutDashboard }, { key: "academias", label: "Academia", icon: Building2 }, { key: "agenda", label: "Agenda", icon: CalendarDays }, { key: "alunos", label: "Alunos", icon: Users }].filter((item) => MODULE_PERMISSIONS[module].includes(item.key));
  return <div className="fixed bottom-0 left-0 right-0 z-30 flex border-t border-[#e4e0d7] bg-white/95 px-2 py-2 backdrop-blur-xl lg:hidden">{items.map((item) => { const Icon = item.icon; return <button key={item.key} onClick={() => setView(item.key as ViewKey)} className={cn("flex flex-1 flex-col items-center gap-1 rounded-xl py-1.5 text-[10px] font-medium", view === item.key ? "text-[#39764d]" : "text-[#9b9488]")}><Icon size={18} strokeWidth={view === item.key ? 2.4 : 1.8} /><span>{item.label}</span></button> })}</div>;
}

function App() {
  const [user, setUser] = useState<UserProfile | null>(() => { const stored = localStorage.getItem("arke-demo-user") ?? sessionStorage.getItem("arke-demo-user"); return ALL_DEMO_USERS.find((item) => item.email === stored) ?? null; });
  const [module, setModule] = useState<ModuleKey>(() => (localStorage.getItem("arke-demo-module") ?? sessionStorage.getItem("arke-demo-module") ?? "administrador") as ModuleKey);
  const [view, setView] = useState<ViewKey>(() => MODULE_VIEWS[(localStorage.getItem("arke-demo-module") ?? sessionStorage.getItem("arke-demo-module") ?? "administrador") as ModuleKey] ?? "overview");
  const [workspace, setWorkspace] = useState(() => localStorage.getItem("arke-demo-workspace") ?? sessionStorage.getItem("arke-demo-workspace") ?? "Rede Arke Demo");
  const [logoUrl, setLogoUrl] = useState(() => localStorage.getItem("arke-demo-logo") ?? sessionStorage.getItem("arke-demo-logo") ?? "/manus-storage/arke-logo-cropped_38a431ec.png");
  const [mobileOpen, setMobileOpen] = useState(false); const [passwordOpen, setPasswordOpen] = useState(false);
  const login = (nextUser: UserProfile, remember: boolean) => { setUser(nextUser); setModule(nextUser.module); setView(MODULE_VIEWS[nextUser.module]); setWorkspace(nextUser.workspace); setLogoUrl(nextUser.logoUrl ?? "/manus-storage/arke-logo-cropped_38a431ec.png"); const storage = remember ? localStorage : sessionStorage; const otherStorage = remember ? sessionStorage : localStorage; storage.setItem("arke-demo-user", nextUser.email); storage.setItem("arke-demo-module", nextUser.module); storage.setItem("arke-demo-workspace", nextUser.workspace); storage.setItem("arke-demo-logo", nextUser.logoUrl ?? "/manus-storage/arke-logo-cropped_38a431ec.png"); otherStorage.removeItem("arke-demo-user"); otherStorage.removeItem("arke-demo-module"); otherStorage.removeItem("arke-demo-workspace"); otherStorage.removeItem("arke-demo-logo"); if (remember) localStorage.setItem("arke-demo-remember", "1"); else localStorage.removeItem("arke-demo-remember"); };
  if (!user) return <LoginScreen onLogin={login} />;
  const logout = () => { localStorage.removeItem("arke-demo-user"); localStorage.removeItem("arke-demo-module"); localStorage.removeItem("arke-demo-workspace"); localStorage.removeItem("arke-demo-remember"); localStorage.removeItem("arke-demo-logo"); sessionStorage.removeItem("arke-demo-user"); sessionStorage.removeItem("arke-demo-module"); sessionStorage.removeItem("arke-demo-workspace"); sessionStorage.removeItem("arke-demo-logo"); setUser(null); };
  const changeWorkspace = (next: string) => { if (user.role !== "Super Admin") return; setWorkspace(next); const client = DEMO_CLIENTS.find((item) => item.name === next); if (client) { setLogoUrl(client.logoUrl); localStorage.setItem("arke-demo-logo", client.logoUrl); sessionStorage.setItem("arke-demo-logo", client.logoUrl); } localStorage.setItem("arke-demo-workspace", next); sessionStorage.setItem("arke-demo-workspace", next); setView("overview"); };
  const changeModule = (next: ModuleKey) => { if (user.role !== "Super Admin") return; setModule(next); setView(MODULE_VIEWS[next]); const storage = localStorage.getItem("arke-demo-user") ? localStorage : sessionStorage; storage.setItem("arke-demo-module", next); };
  return <div className="flex min-h-screen bg-[#f7f5f0] text-[#2b271f]"><Sidebar view={view} setView={setView} user={user} workspace={workspace} module={module} onModuleChange={changeModule} onPasswordChange={() => setPasswordOpen(true)} onLogout={logout} />{mobileOpen && <div className="fixed inset-0 z-40 bg-[#15130f]/30 lg:hidden" onClick={() => setMobileOpen(false)}><div className="h-full w-[282px] bg-[#fffdf9] p-4 shadow-2xl" onClick={(event) => event.stopPropagation()}><div className="mb-5 flex items-center justify-between"><div className="flex items-center gap-3 px-2"><div className="flex h-9 w-9 items-center justify-center overflow-hidden rounded-xl bg-black"><img src={user.logoUrl ?? "/manus-storage/arke-logo-cropped_38a431ec.png"} alt="Logo do cliente" className="h-full w-full object-cover" /></div><span className="text-lg font-black tracking-[-.06em]">ARKE</span></div><button onClick={() => setMobileOpen(false)} className="rounded-lg p-2 text-[#76847b]"><X size={18} /></button></div><div className="mb-5"><label className="mb-2 block px-2 text-[10px] font-bold uppercase tracking-[.15em] text-[#aaa193]">Módulo do cliente</label><div className="rounded-xl bg-[#faf3df] px-3 py-2 text-xs font-semibold text-[#314238]">{MODULE_LABELS[module]}</div></div><div className="space-y-6">{navGroups.map((group) => { const items = group.items.filter((item) => MODULE_PERMISSIONS[module].includes(item.key)); return items.length ? <div key={group.label}><p className="mb-2 px-3 text-[10px] font-bold uppercase tracking-[.15em] text-[#aaa193]">{group.label}</p>{items.map((item) => { const Icon = item.icon; return <button key={item.key} onClick={() => { setView(item.key as ViewKey); setMobileOpen(false); }} className={cn("flex w-full items-center gap-3 rounded-xl px-3 py-3 text-left text-sm", view === item.key ? "bg-[#15130f] font-semibold text-white" : "text-[#766f62]")}><Icon size={17} />{item.label}</button> })}</div> : null; })}</div></div></div>}<div className="flex min-w-0 flex-1 flex-col"><Topbar view={view} user={user} workspace={workspace} onMenu={() => setMobileOpen(true)} onLogout={logout} /><main className="min-w-0 flex-1 overflow-y-auto pb-20 lg:pb-0"><Home view={view} setView={setView} user={user} module={module} logoUrl={logoUrl} tenantName={workspace} onTenantChange={changeWorkspace} /></main><MobileNav view={view} setView={setView} module={module} />{passwordOpen && <PasswordDialog user={user} onClose={() => setPasswordOpen(false)} />}</div></div>;
}

export default App;
