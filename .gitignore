node_modules
dist
dist-ssr
.env
*.local
.DS_Store
*.tsbuildinfo

# Artefatos locais do Supabase CLI (versão baixada em cache), não versionar
supabase/.temp/
.vercel
.env*
# O .env.example é o modelo versionado das variáveis; o .env* acima não pode
# escondê-lo (o vercel link acrescentou .vercel e .env* automaticamente).
!.env.example

# Artefatos do Playwright (e2e/)
test-results/
playwright-report/

# Binários da ponte Topdata (gerados por packages/ponte-topdata/build.ps1)
packages/ponte-topdata/bin/
scripts/ajuda/.sessoes.json
